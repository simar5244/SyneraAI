// Empty stub - all vector functionality removed

/**
 * Placeholder for vector search setup that does nothing
 * @returns Always returns true to not block the signup flow
 */
export async function setupVectorSearch(): Promise<boolean> {
  return true;
}

/**
 * Placeholder for vector indexes check (does nothing)
 */
export async function checkVectorIndexes(): Promise<boolean> {
  return true;
}

/**
 * Placeholder for vector index creation (does nothing)
 */
export async function createVectorIndex(): Promise<boolean> {
  return true;
} 